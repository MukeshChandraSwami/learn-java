package java17.switch_enhancement;

public record Circle(float radius) implements GeometricalCalculation {

    @Override
    public double area() {
        return Math.PI * Math.pow(radius, 2);
    }

    @Override
    public double perimeter() {
        return 2 * Math.PI * radius;
    }
}
